{
  "name": "write_tickets_tool",
  "summary": [
    "Mutate the ticket queue: create new tickets or edit fields.",
    "Use `create` with `labels` containing an agent's name to delegate work to that agent, or with a topical label to scope it.",
    "Finishing a ticket is done with `write_result_tool`, not this tool."
  ],
  "constraints": [
    "One `action` per call: `create` or `edit`.",
    "`edit` defaults `key` to the agent's current ticket — omit `key` to act on your own work.",
    "`create` stamps `reporter` from the calling agent automatically; there is no `reporter` parameter."
  ],
  "anti_patterns": [
    "To look up, list, or search tickets — use `read_tickets_tool`.",
    "When the agent needs read AND write in the same flow — use `manage_tickets_tool` instead of registering both.",
    "To finish the agent's current ticket — call `write_result_tool`.",
    "To delegate work to another agent — use `create` with that agent's name in `labels`; the loop routes label-named tickets directly to the matching agent."
  ],
  "cautions": [
    "IMPORTANT: this tool cannot transition status. Use `write_result_tool` to finish; `Failed` is reserved for ticket-system-driven outcomes (schema-retry trip, policy violations).",
    "ALWAYS finish the current ticket with `write_result_tool` before the response ends. Returning text without a result leaves the ticket `InProgress` and the loop will re-pick it next iteration."
  ],
  "output": [
    "Plain text confirmation: `Created ticket KEY` or `Edited ticket KEY`.",
    "On rejection: an error describing the failed action (missing field, unknown ticket)."
  ],
  "read_only": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Write mode: `create` or `edit`."
      },
      "key": {
        "type": "string",
        "description": "Ticket key (e.g. `TICKET-3`). Required for `edit` (defaults to current ticket); ignored by `create`."
      },
      "task": {
        "description": "For `create` (required): the task body — any JSON value (string, object, array, scalar). For `edit` (optional): replace the task body."
      },
      "labels": {
        "type": "array",
        "items": { "type": "string" },
        "description": "For `create` or `edit` (optional): label scope. Determines which agents pick up the ticket. Including an agent's name as a label pins the ticket to that agent."
      },
      "schema": {
        "type": "object",
        "description": "For `create` or `edit` (optional): a JSON Schema document. When set, the agent's final result (written via `write_result_tool`) must validate against it; failures count toward `max_schema_retries`."
      }
    },
    "required": ["action"]
  }
}
