[package]
name = "use-cases"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
agent = { path = "../agent" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal"] }
serde_json = "1"

[[bin]]
name = "project-scanner"
path = "src/cli/project_scanner.rs"
